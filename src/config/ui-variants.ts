// Configurazione UI per la gestione dell'autenticazione
// Variante C+E: Card-hero + badge lock + banner sticky

export const UI_CONFIG = {
  name: 'Card-Hero + Badge Lock + Banner Sticky',
  description: 'Prima card speciale + lock minimali + banner sticky',
  features: [
    'Card-hero prominente in prima posizione',
    'Badge lock sui pulsanti sensibili',
    'Banner sticky dopo scroll',
    'Griglia sempre visibile con blur sui nomi'
  ]
};
